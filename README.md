# metadata_extractor
metadata extractor for rental agreements uploads .docx or scanned images, uses Google Gemini (multimodal LLM) to extract key fields (rent, dates, parties, notice period) as structured JSON, with a FastAPI web UI and 88% recall on test set.
